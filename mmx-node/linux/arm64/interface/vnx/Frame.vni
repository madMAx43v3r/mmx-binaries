package vnx;

class Frame extends Message {
	
	Hash64 src_mac;				// source address
	
	Hash64 dst_mac;				// destination address
	
	ulong seq_num;				// first sequence number is at least 1
	
	Value* value;				// the actual data
	
}
