package vnx;


class JRPC_Request extends JRPC_Notification{
	Variant id;                           // String, Number or null (discouraged)
	
	bool is_request_object() const;       // true if it is a request or a notification
	bool is_request() const;              // true if it is a request
	bool is_notification() const;         // true if it is a notification
}

