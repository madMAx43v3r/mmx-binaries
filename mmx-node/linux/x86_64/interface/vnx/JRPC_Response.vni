package vnx;


class JRPC_Response{
	string jsonrpc;                      // must be "2.0"
	Variant id;                          // same as in the request
	
	bool is_response_object() const;     // true if it is a response (success or failure)
}

