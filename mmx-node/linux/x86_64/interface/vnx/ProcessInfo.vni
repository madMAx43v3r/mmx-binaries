package vnx;

class ProcessInfo {
	
	long time;			// time stamp (virtual time) [usec]
	
	Hash64 id;			// process id
	
	string name;		// process name
	
	vector<TopicInfo> topics;
	
	vector<ModuleInfo> modules;
	
}
